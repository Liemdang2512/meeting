---
phase: 11-s-a-giao-th-c-t-nh-ti-n-sau-ng-k-t-i-kho-n-c-s-d-c-nh-s-d-ng-s-tr-tr-c-ti-p-v-o-s-d
plan: 05
type: execute
wave: 5
depends_on: [11-01, 11-02, 11-03]
files_modified:
  - server/routes/wallet.ts
  - server/index.ts
  - features/pricing/CreditHistory.tsx
  - features/pricing/PricingPage.tsx
autonomous: false
requirements: [TBD]
requirements_trace:
  - "Phase 11 wallet ledger schema already captures all transactions (debit/topup/refund) in wallet_ledger table."
must_haves:
  truths:
    - "User can view their own credit transaction history (debit, topup, refund) sorted newest-first."
    - "Each row shows: date, event_type label (human-readable), action_type (if debit), amount_credits, balance_after_credits."
    - "API only returns rows belonging to authenticated user — no cross-user data leak."
  artifacts:
    - path: "server/routes/wallet.ts"
      provides: "GET /api/wallet/history — paginated wallet_ledger rows for authenticated user"
    - path: "features/pricing/CreditHistory.tsx"
      provides: "UI component: transaction history table with pagination"
    - path: "features/pricing/PricingPage.tsx"
      provides: "Renders CreditHistory as a section below wallet balance"
  key_links:
    - from: "features/pricing/PricingPage.tsx"
      to: "server/routes/wallet.ts"
      via: "fetch /api/wallet/history on mount"
      pattern: "GET|wallet/history"
---

<objective>
Hiển thị lịch sử giao dịch credit cho user — tận dụng wallet_ledger đã có sẵn từ Phase 11-01.

Purpose: user thấy được từng lần credit bị trừ (debit) hoặc nạp (topup), giúp kiểm tra tài khoản minh bạch.
Output: API endpoint + UI table hiển thị trong PricingPage.
</objective>

<context>
@.planning/phases/11-s-a-giao-th-c-t-nh-ti-n-sau-ng-k-t-i-kho-n-c-s-d-c-nh-s-d-ng-s-tr-tr-c-ti-p-v-o-s-d/11-01-PLAN.md
@server/routes/quota.ts
@features/pricing/PricingPage.tsx
@features/pricing/QuotaBadge.tsx
@db/schema.sql
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: API endpoint GET /api/wallet/history</name>
  <read_first>
    - db/schema.sql (wallet_ledger table columns)
    - server/routes/quota.ts (auth pattern, sql import)
    - server/index.ts (route mounting)
  </read_first>
  <files>server/routes/wallet.ts, server/index.ts</files>
  <acceptance_criteria>
    - GET /api/wallet/history yêu cầu auth (requireAuth middleware).
    - Query wallet_ledger WHERE user_id = req.user.id ORDER BY created_at DESC.
    - Hỗ trợ pagination: query param ?page=1&pageSize=20 (default pageSize=20, max=50).
    - Response JSON: { items: [...], total: number, page: number, pageSize: number }.
    - Mỗi item trả về: id, event_type, action_type, amount_credits, balance_after_credits, created_at.
    - Không trả dữ liệu của user khác (WHERE user_id = req.user.id bắt buộc).
  </acceptance_criteria>
  <action>
    Tạo file server/routes/wallet.ts với Router Express:
    - import sql từ db/index.ts
    - import requireAuth từ middleware
    - GET / với pagination logic
    - Mount tại /api/wallet trong server/index.ts
  </action>
  <verify>
    <manual>curl -H "Authorization: Bearer {token}" http://localhost:3001/api/wallet/history trả về JSON hợp lệ</manual>
  </verify>
  <done>Endpoint hoạt động, trả đúng dữ liệu cho user đang đăng nhập.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: CreditHistory UI component + tích hợp vào PricingPage</name>
  <read_first>
    - features/pricing/PricingPage.tsx
    - features/pricing/QuotaBadge.tsx (style reference)
  </read_first>
  <files>features/pricing/CreditHistory.tsx, features/pricing/PricingPage.tsx</files>
  <acceptance_criteria>
    - CreditHistory component fetch GET /api/wallet/history khi mount.
    - Hiển thị table/list với các cột: Thời gian, Loại, Hành động, Số credits, Số dư sau.
    - event_type được hiển thị bằng tiếng Việt: debit→"Trừ tiền", topup→"Nạp tiền", refund→"Hoàn tiền", migration_grant→"Credit khởi tạo".
    - amount_credits âm (debit) hiển thị màu đỏ, dương (topup/refund) hiển thị màu xanh.
    - Có nút "Xem thêm" nếu còn trang tiếp theo.
    - Loading state và empty state ("Chưa có giao dịch nào").
    - PricingPage render CreditHistory bên dưới phần hiển thị số dư.
  </acceptance_criteria>
  <action>
    Tạo features/pricing/CreditHistory.tsx:
    - useState cho items, loading, page
    - useEffect fetch /api/wallet/history?page=1&pageSize=20
    - Table với Tailwind styling nhất quán với PricingPage hiện tại
    - Format date bằng toLocaleDateString('vi-VN')
    Cập nhật PricingPage.tsx: import và render CreditHistory sau wallet balance section.
  </action>
  <verify>
    <manual>Mở trang Pricing/Nâng cấp trong browser, cuộn xuống thấy bảng lịch sử giao dịch.</manual>
  </verify>
  <done>User thấy lịch sử credit trực tiếp trong trang Pricing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify lịch sử giao dịch</name>
  <files>features/pricing/CreditHistory.tsx, features/pricing/PricingPage.tsx</files>
  <acceptance_criteria>
    - Trang Pricing hiển thị bảng lịch sử phía dưới số dư.
    - Sau khi tạo biên bản hoặc sơ đồ tư duy, reload trang thấy row mới với event_type=debit.
    - Số dư hiển thị đúng khớp với balance_after_credits của row cuối cùng.
  </acceptance_criteria>
  <how-to-verify>
    1. Đăng nhập, vào trang Pricing → thấy bảng lịch sử.
    2. Tạo 1 biên bản mới → quay lại Pricing → thấy row debit mới.
    3. Kiểm tra số dư hiển thị khớp với cột "Số dư sau" của row mới nhất.
  </how-to-verify>
  <resume-signal>Gõ "approved" nếu hoạt động đúng, hoặc mô tả lỗi cụ thể.</resume-signal>
  <done>User xác nhận lịch sử hiển thị chính xác và cập nhật sau mỗi giao dịch.</done>
</task>

</tasks>

<verification>
- npm run build (TypeScript compile clean)
- curl test /api/wallet/history
</verification>

<success_criteria>
- User thấy lịch sử credit trong trang Pricing.
- API bảo mật — chỉ trả data của user đang đăng nhập.
- Không cần DB migration — dùng wallet_ledger đã có.
</success_criteria>

<output>
After completion, create `.planning/phases/11-s-a-giao-th-c-t-nh-ti-n-sau-ng-k-t-i-kho-n-c-s-d-c-nh-s-d-ng-s-tr-tr-c-ti-p-v-o-s-d/11-05-SUMMARY.md`
</output>
