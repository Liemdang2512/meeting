---
phase: 07-email-sending-after-minutes
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - features/minutes/components/EmailRecipientsInput.test.tsx
  - features/minutes/storage.test.ts
  - server/lib/__tests__/pdfGenerator.test.ts
  - server/lib/__tests__/emailTemplate.test.ts
  - server/routes/__tests__/email.test.ts
autonomous: true
requirements: [EMAIL-01, EMAIL-02, EMAIL-03, EMAIL-04]

must_haves:
  truths:
    - "vitest run finds and executes test stub files without config errors"
    - "All test stubs fail with meaningful TODO assertions (RED phase)"
  artifacts:
    - path: "features/minutes/components/EmailRecipientsInput.test.tsx"
      provides: "Test stubs for chips input validation, add/remove, max limit"
      contains: "describe.*EmailRecipientsInput"
    - path: "features/minutes/storage.test.ts"
      provides: "Test stubs for recipientEmails exclusion from draft"
      contains: "describe.*storage"
    - path: "server/lib/__tests__/pdfGenerator.test.ts"
      provides: "Test stubs for PDF buffer generation"
      contains: "describe.*generateMinutesPdfBuffer"
    - path: "server/lib/__tests__/emailTemplate.test.ts"
      provides: "Test stubs for HTML email template builder"
      contains: "describe.*buildEmailHtml"
    - path: "server/routes/__tests__/email.test.ts"
      provides: "Test stubs for email route + admin settings endpoints"
      contains: "describe.*email"
  key_links: []
---

<objective>
Create test stub files for all Phase 7 features so that subsequent implementation plans have behavioral verification commands. Each stub contains describe/it blocks with `expect(true).toBe(false)` placeholder assertions that will turn GREEN as implementation lands.

Purpose: Satisfies Nyquist rule — every implementation task must have a behavioral test gate. These stubs define the test contract; Plans 01-03 will make them pass.

Output: 5 test files with failing stubs
</objective>

<execution_context>
@/Users/tanliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-email-sending-after-minutes/07-CONTEXT.md
@.planning/phases/07-email-sending-after-minutes/07-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create frontend test stubs (EmailRecipientsInput + storage)</name>
  <files>features/minutes/components/EmailRecipientsInput.test.tsx, features/minutes/storage.test.ts</files>
  <read_first>
    - features/minutes/storage.ts (current API to understand test targets)
    - .planning/phases/07-email-sending-after-minutes/07-RESEARCH.md (Validation Architecture section — test map)
    - vitest.config.ts or vite.config.ts (test configuration)
  </read_first>
  <action>
1. Create `features/minutes/components/EmailRecipientsInput.test.tsx`:

```typescript
import { describe, it, expect } from 'vitest';

describe('EmailRecipientsInput', () => {
  it('adds valid email on Enter key', () => {
    // TODO: implement when EmailRecipientsInput component exists
    expect(true).toBe(false);
  });

  it('adds valid email on comma key', () => {
    expect(true).toBe(false);
  });

  it('rejects invalid email format with error message', () => {
    expect(true).toBe(false);
  });

  it('rejects duplicate email with error message', () => {
    expect(true).toBe(false);
  });

  it('enforces max 20 recipients limit', () => {
    expect(true).toBe(false);
  });

  it('removes last chip on Backspace when input is empty', () => {
    expect(true).toBe(false);
  });

  it('removes specific chip when delete button clicked', () => {
    expect(true).toBe(false);
  });

  it('auto-dismisses error after 3 seconds', () => {
    expect(true).toBe(false);
  });
});
```

2. Create `features/minutes/storage.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';

describe('storage', () => {
  describe('saveMeetingInfoDraft', () => {
    it('does NOT persist recipientEmails to localStorage', () => {
      // TODO: implement when storage.ts is updated with recipientEmails exclusion
      expect(true).toBe(false);
    });

    it('persists all other MeetingInfo fields to localStorage', () => {
      expect(true).toBe(false);
    });
  });

  describe('loadMeetingInfoDraft', () => {
    it('returns recipientEmails as empty array', () => {
      expect(true).toBe(false);
    });
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run features/minutes/components/EmailRecipientsInput.test.tsx features/minutes/storage.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>Both test files exist, vitest discovers them, all tests fail (RED state) — ready for Plans 01-02 to make them GREEN</done>
</task>

<task type="auto">
  <name>Task 2: Create backend test stubs (pdfGenerator + emailTemplate + email route)</name>
  <files>server/lib/__tests__/pdfGenerator.test.ts, server/lib/__tests__/emailTemplate.test.ts, server/routes/__tests__/email.test.ts</files>
  <read_first>
    - .planning/phases/07-email-sending-after-minutes/07-RESEARCH.md (Pattern 1: jsPDF, Pattern 2: Resend, Pattern 3: admin settings)
    - server/routes/admin.ts (existing route pattern for reference)
  </read_first>
  <action>
1. Create directory `server/lib/__tests__/` and `server/routes/__tests__/` if they don't exist.

2. Create `server/lib/__tests__/pdfGenerator.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';

describe('generateMinutesPdfBuffer', () => {
  it('returns a Buffer', () => {
    // TODO: implement when pdfGenerator.ts exists
    expect(true).toBe(false);
  });

  it('Buffer starts with PDF magic bytes (%PDF)', () => {
    expect(true).toBe(false);
  });

  it('handles empty participants array', () => {
    expect(true).toBe(false);
  });

  it('handles long markdown content with page breaks', () => {
    expect(true).toBe(false);
  });
});
```

3. Create `server/lib/__tests__/emailTemplate.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';

describe('buildEmailHtml', () => {
  it('returns valid HTML string with DOCTYPE', () => {
    // TODO: implement when emailTemplate.ts exists
    expect(true).toBe(false);
  });

  it('uses only inline styles (no style blocks)', () => {
    expect(true).toBe(false);
  });

  it('includes company name in header', () => {
    expect(true).toBe(false);
  });

  it('includes participants table', () => {
    expect(true).toBe(false);
  });

  it('includes minutes HTML content', () => {
    expect(true).toBe(false);
  });

  it('escapes HTML special characters in user data', () => {
    expect(true).toBe(false);
  });
});
```

4. Create `server/routes/__tests__/email.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';

describe('POST /api/email/send-minutes', () => {
  it('returns 400 if recipients array is empty', () => {
    // TODO: implement when email route exists
    expect(true).toBe(false);
  });

  it('returns 400 if subject is missing', () => {
    expect(true).toBe(false);
  });

  it('returns 503 if Resend API key not configured in DB', () => {
    expect(true).toBe(false);
  });

  it('calls Resend SDK with correct payload including PDF attachment', () => {
    expect(true).toBe(false);
  });

  it('returns success response with email ID', () => {
    expect(true).toBe(false);
  });
});

describe('GET /api/admin/settings', () => {
  it('returns settings with masked API key', () => {
    expect(true).toBe(false);
  });
});

describe('PUT /api/admin/settings', () => {
  it('rejects unknown setting keys', () => {
    expect(true).toBe(false);
  });

  it('upserts allowed setting keys', () => {
    expect(true).toBe(false);
  });
});
```
  </action>
  <verify>
    <automated>npx vitest run server/lib/__tests__/pdfGenerator.test.ts server/lib/__tests__/emailTemplate.test.ts server/routes/__tests__/email.test.ts 2>&1 | tail -5</automated>
  </verify>
  <done>All 3 backend test files exist, vitest discovers them, all tests fail (RED state) — ready for Plan 03 to make them GREEN</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose 2>&1 | grep -c "FAIL"` shows all stub tests failing
- All 5 test files are discoverable by vitest
- No import errors (stubs only import from vitest, not from implementation files yet)
</verification>

<success_criteria>
1. 5 test stub files created in correct locations
2. vitest discovers and runs all stubs
3. All tests fail with `expect(true).toBe(false)` (RED state)
4. No test stubs import implementation modules (they will be updated in Plans 01-03)
</success_criteria>

<output>
After completion, create `.planning/phases/07-email-sending-after-minutes/07-00-SUMMARY.md`
</output>
