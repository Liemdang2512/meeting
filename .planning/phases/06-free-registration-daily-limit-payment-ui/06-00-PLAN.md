---
phase: 06-free-registration-daily-limit-payment-ui
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - components/__tests__/RegisterPage.test.tsx
  - components/__tests__/PricingPage.test.tsx
  - components/__tests__/UpgradeModal.test.tsx
  - features/pricing/__tests__/QuotaBadge.test.tsx
autonomous: true
requirements:
  - REG-01
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "All 4 stub test files exist and vitest can collect them without errors"
    - "npm run test:unit passes (stubs are pending, not failing)"
  artifacts:
    - path: "components/__tests__/RegisterPage.test.tsx"
      provides: "Test scaffold for RegisterPage component"
    - path: "components/__tests__/PricingPage.test.tsx"
      provides: "Test scaffold for PricingPage component"
    - path: "components/__tests__/UpgradeModal.test.tsx"
      provides: "Test scaffold for UpgradeModal component"
    - path: "features/pricing/__tests__/QuotaBadge.test.tsx"
      provides: "Test scaffold for QuotaBadge component"
  key_links:
    - from: "components/__tests__/RegisterPage.test.tsx"
      to: "components/RegisterPage.tsx"
      via: "import { RegisterPage } from '../RegisterPage'"
      pattern: "RegisterPage"
---

<objective>
Create minimal stub test files for the 4 new components introduced in Phase 06. These stubs satisfy the Nyquist rule: every task with tdd="true" must have a test file that can be collected, even before implementation exists.

Purpose: Plans 06-03, 06-04, and 06-05 use tdd="true" on tasks that create RegisterPage, PricingPage, UpgradeModal, and QuotaBadge. Without test files, npm run test:unit either skips those components entirely or errors on missing imports. Wave 0 creates pending stubs that pass immediately, then later tasks fill in real assertions.

Output:
- 4 stub test files (pending describe blocks, no implementation assertions)
- npm run test:unit green after this plan
</objective>

<execution_context>
@/Users/tanliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-free-registration-daily-limit-payment-ui/06-CONTEXT.md

<interfaces>
<!-- Existing test file format — match exactly. From components/__tests__/LoginPage.test.tsx -->

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import { vi } from 'vitest';

// vi.mock any modules the component imports that are not yet implemented

describe('ComponentName', () => {
  it.todo('behavior description');
  it.todo('another behavior');
});
```

Note: Use `it.todo('description')` for pending tests — vitest marks them as pending (not failing).
Do NOT use `it('description', () => { /* empty */ })` — that passes vacuously but hides intent.
Do NOT use `xit` or `xdescribe` — those are Jest patterns; use `it.todo` in vitest.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Stub test files for RegisterPage, PricingPage, UpgradeModal</name>
  <files>components/__tests__/RegisterPage.test.tsx, components/__tests__/PricingPage.test.tsx, components/__tests__/UpgradeModal.test.tsx</files>
  <action>
Create `components/__tests__/RegisterPage.test.tsx`:

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import { vi } from 'vitest';

vi.mock('../../lib/auth', () => ({
  register: vi.fn(),
}));

describe('RegisterPage', () => {
  it.todo('hiển thị form đăng ký với 3 trường: Email, Mật khẩu, Xác nhận mật khẩu');
  it.todo('hiển thị lỗi khi mật khẩu ngắn hơn 8 ký tự (client-side, trước khi gọi API)');
  it.todo('hiển thị lỗi khi mật khẩu xác nhận không khớp (client-side)');
  it.todo('gọi register() và onRegisterSuccess khi form hợp lệ');
  it.todo('hiển thị "Đang đăng ký..." và disable nút submit khi đang loading');
  it.todo('hiển thị lỗi từ server khi register() throw');
  it.todo('có link "Đã có tài khoản? Đăng nhập" gọi onGoToLogin');
});
```

Create `components/__tests__/PricingPage.test.tsx`:

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import { vi } from 'vitest';

describe('PricingPage', () => {
  it.todo('hiển thị 3 gói: Free, Pro, Enterprise');
  it.todo('gói Free có nút "Gói hiện tại" bị disabled khi currentUserRole="free"');
  it.todo('gói Pro có nút "Nâng cấp ngay" mở UpgradeModal khi click');
  it.todo('gói Enterprise có nút "Liên hệ"');
  it.todo('UpgradeModal không hiển thị khi mới render (isOpen=false)');
});
```

Create `components/__tests__/UpgradeModal.test.tsx`:

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import { vi } from 'vitest';

describe('UpgradeModal', () => {
  it.todo('không render gì khi isOpen=false');
  it.todo('hiển thị form thanh toán khi isOpen=true (step: form)');
  it.todo('form có trường: cardNumber, expiry, CVV');
  it.todo('submit form → hiển thị "Đang xử lý..." (step: processing)');
  it.todo('sau 2 giây → hiển thị "Thanh toán thành công!" (step: success)');
  it.todo('nút Đóng trong success state gọi onClose và reset về form');
});
```
  </action>
  <verify>
    <automated>npx vitest run components/__tests__/RegisterPage.test.tsx components/__tests__/PricingPage.test.tsx components/__tests__/UpgradeModal.test.tsx --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - All 3 files exist in components/__tests__/
    - vitest can collect and run them (all tests show as "todo", none as "failed")
    - No import errors or TypeScript errors in stub files
  </done>
</task>

<task type="auto">
  <name>Task 2: Stub test file for QuotaBadge</name>
  <files>features/pricing/__tests__/QuotaBadge.test.tsx</files>
  <action>
First, ensure the directory exists:
```bash
mkdir -p features/pricing/__tests__
```

Create `features/pricing/__tests__/QuotaBadge.test.tsx`:

```typescript
import React from 'react';
import { render, screen } from '@testing-library/react';
import { vi } from 'vitest';

vi.mock('../../../lib/api', () => ({
  authFetch: vi.fn(),
}));

describe('QuotaBadge', () => {
  it.todo('không render gì khi quota chưa load (null state)');
  it.todo('hiển thị "Unlimited" khi role !== "free" (unlimited: true)');
  it.todo('hiển thị "Hôm nay: 0/1 lượt" khi used=0, limit=1 (màu xanh)');
  it.todo('hiển thị "Hôm nay: 1/1 lượt" khi used=1, limit=1 (màu amber)');
  it.todo('gọi onQuotaExhausted khi remaining === 0');
  it.todo('re-fetch quota khi nhận được sự kiện "quota-updated" trên window');
});
```
  </action>
  <verify>
    <automated>npx vitest run features/pricing/__tests__/QuotaBadge.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    - features/pricing/__tests__/QuotaBadge.test.tsx exists
    - vitest collects it without errors (all tests pending/todo)
    - features/pricing/ directory exists (needed by plan 06-04 and 06-05)
  </done>
</task>

</tasks>

<verification>
- `npm run test:unit` passes (all stub tests are pending, not failing)
- All 4 stub files exist:
  - `components/__tests__/RegisterPage.test.tsx`
  - `components/__tests__/PricingPage.test.tsx`
  - `components/__tests__/UpgradeModal.test.tsx`
  - `features/pricing/__tests__/QuotaBadge.test.tsx`
- `npx tsc --noEmit` passes (no TypeScript errors in stub files)
</verification>

<success_criteria>
- npm run test:unit green after this plan (stubs are pending, not red)
- 4 test files exist matching project vitest + React Testing Library pattern
- features/pricing/__tests__/ directory exists, unblocking plan 06-04 component creation
</success_criteria>

<output>
After completion, create `.planning/phases/06-free-registration-daily-limit-payment-ui/06-00-SUMMARY.md`
</output>
