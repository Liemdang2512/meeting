---
phase: 09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang
plan: 03
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - features/workflows/reporter/ReporterInfoForm.tsx
  - features/workflows/officer/OfficerInfoForm.tsx
  - components/MeetingLandingPage.tsx
autonomous: true
requirements: [UI-06, UI-07, UI-08]

must_haves:
  truths:
    - "ReporterInfoForm renders all 5 fields from D-05: interviewTitle, guestName, reporter, datetime, location"
    - "OfficerInfoForm renders all 6 fields from D-07: title, presiding, courtSecretary, participants, datetime, location"
    - "MeetingLandingPage renders workflow cards filtered by user.workflowGroups per D-01"
  artifacts:
    - path: "features/workflows/reporter/ReporterInfoForm.tsx"
      provides: "Reporter form component with D-05 fields"
      exports: ["ReporterInfoForm"]
    - path: "features/workflows/officer/OfficerInfoForm.tsx"
      provides: "Officer form component with D-07 fields"
      exports: ["OfficerInfoForm"]
    - path: "components/MeetingLandingPage.tsx"
      provides: "Group card selection page for /meeting route"
      exports: ["MeetingLandingPage"]
  key_links:
    - from: "features/workflows/reporter/ReporterInfoForm.tsx"
      to: "features/minutes/types.ts"
      via: "import ReporterInfo"
      pattern: "import.*ReporterInfo"
    - from: "features/workflows/officer/OfficerInfoForm.tsx"
      to: "features/minutes/types.ts"
      via: "import OfficerInfo, MeetingParticipant"
      pattern: "import.*OfficerInfo.*MeetingParticipant"
    - from: "components/MeetingLandingPage.tsx"
      to: "App.tsx"
      via: "WORKFLOW_CARDS constant + navigate function"
      pattern: "navigate.*reporter|specialist|officer"
---

<objective>
Build the 3 form components (ReporterInfoForm, OfficerInfoForm) and the MeetingLandingPage card selection page.

Purpose: UI layer for data entry and workflow selection — consumed by workflow page components in Plan 04/05/06.
Output: 2 form components + 1 landing page component.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-CONTEXT.md
@.planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-RESEARCH.md
@.planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-UI-SPEC.md
@.planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 -->
From features/minutes/types.ts (after Plan 01):
```typescript
export interface ReporterInfo {
  interviewTitle: string; guestName: string; reporter: string; datetime: string; location: string;
}
export interface OfficerInfo {
  title: string; presiding: string; courtSecretary: string;
  participants: MeetingParticipant[]; datetime: string; location: string;
}
export interface MeetingParticipant { id: string; name: string; title?: string; role?: MeetingParticipantRole; }
```

From features/minutes/components/MeetingInfoForm.tsx (reference for form pattern):
```typescript
type Props = {
  initialValue: MeetingInfo;
  onChange: (next: MeetingInfo) => void;
  onContinue: () => void;
  onSkip: () => void;
};
// Uses: controlled inputs, updateField helper, participant add/update/remove
// Auto-saves via useEffect on value change
```

From App.tsx:
```typescript
const WORKFLOW_CARDS: { group: WorkflowGroup; label: string; description: string }[] = [
  { group: 'reporter', label: 'Bài phỏng vấn', description: 'Ghi chép & tổng hợp phỏng vấn báo chí' },
  { group: 'specialist', label: 'Thư ký họp', description: 'Biên bản cuộc họp chuyên nghiệp' },
  { group: 'officer', label: 'Thông tin vụ án', description: 'Ghi chép hồ sơ pháp lý' },
];
```

From lib/auth.ts:
```typescript
interface AuthUser { userId: string; email: string; role: string; workflowGroups?: string[]; activeWorkflowGroup?: string; }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ReporterInfoForm and OfficerInfoForm components</name>
  <files>features/workflows/reporter/ReporterInfoForm.tsx, features/workflows/officer/OfficerInfoForm.tsx</files>
  <read_first>
    - features/minutes/components/MeetingInfoForm.tsx (reference form pattern — controlled inputs, participant management, auto-save)
    - features/minutes/types.ts (for ReporterInfo, OfficerInfo, MeetingParticipant types)
    - features/minutes/storage.ts (for saveReporterDraft, saveOfficerDraft)
    - .planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-UI-SPEC.md (Form Fields section)
    - .planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-CONTEXT.md (D-05, D-07)
  </read_first>
  <action>
    **Per D-04:** Each group has its own form (NOT conditional logic in MeetingInfoForm).

    **ReporterInfoForm (D-05)** — `features/workflows/reporter/ReporterInfoForm.tsx`:
    Props: `{ value: ReporterInfo; onChange: (next: ReporterInfo) => void; onContinue: () => void; onSkip: () => void; }`

    Fields (all text inputs except datetime):
    - "Tiêu đề phỏng vấn" → `interviewTitle` (text, placeholder "VD: Phỏng vấn CEO công ty ABC")
    - "Tên khách mời" → `guestName` (text, placeholder "VD: Nguyễn Văn A")
    - "Phóng viên" → `reporter` (text, placeholder "VD: Trần Thị B")
    - "Thời gian" → `datetime` (datetime-local input)
    - "Địa điểm" → `location` (text, placeholder "VD: Trụ sở ABC, Quận 1, TP.HCM")

    Grid: `grid-cols-1 md:grid-cols-2 gap-4` — interviewTitle spans full width (`md:col-span-2`), rest in 2-col grid.

    Auto-save: `useEffect(() => { saveReporterDraft(value); }, [value])` — import saveReporterDraft from storage.ts.

    Form section wrapper: `bg-white border border-slate-200 rounded-xl p-6 space-y-6 shadow-sm`
    Heading: "Thông tin phỏng vấn" per 09-UI-SPEC.md copywriting.
    Input classes: `w-full px-4 py-3 border border-slate-200 rounded-xl text-sm font-normal focus:outline-none focus:ring-2 focus:ring-[#1E40AF]/30 focus:border-[#1E40AF] bg-white`
    Label classes: `block text-sm font-semibold text-slate-800 mb-1`
    Primary CTA: "Tiếp tục tạo bài" per UI-SPEC copywriting. Classes: `px-6 py-3 bg-[#1E3A8A] text-white font-semibold rounded-xl hover:bg-[#1E40AF] transition-colors duration-200 shadow-sm`
    Skip button: "Bỏ qua". Classes: `px-6 py-3 border border-slate-200 text-slate-600 font-semibold rounded-xl hover:bg-slate-50 transition-colors duration-200`

    **OfficerInfoForm (D-07)** — `features/workflows/officer/OfficerInfoForm.tsx`:
    Props: `{ value: OfficerInfo; onChange: (next: OfficerInfo) => void; onContinue: () => void; onSkip: () => void; }`

    Fields:
    - "Tiêu đề" → `title` (text, spans full width)
    - "Chủ Toạ" → `presiding` (text)
    - "Thư ký toà" → `courtSecretary` (text)
    - "Thời gian" → `datetime` (datetime-local)
    - "Địa điểm" → `location` (text)
    - "Nhân sự" → `participants` (MeetingParticipant[] — reuse the exact same participant management pattern from MeetingInfoForm: add/update/remove with createId(), ROLE_OPTIONS array, name/title/role inputs per row)

    Auto-save: `useEffect(() => { saveOfficerDraft(value); }, [value])`

    Form heading: "Thông tin vụ án" per UI-SPEC copywriting.
    Primary CTA: "Tiếp tục tạo hồ sơ" per UI-SPEC copywriting.
    Same styling classes as ReporterInfoForm.

    **Important:** Copy the participant management logic (addParticipant, updateParticipant, removeParticipant, ROLE_OPTIONS, createId) from MeetingInfoForm into OfficerInfoForm. Do NOT import from MeetingInfoForm — keep it self-contained per D-04 to avoid coupling. This duplicates ~30 lines but keeps forms independent.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function ReporterInfoForm" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "interviewTitle" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "guestName" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "reporter" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "datetime" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "location" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "saveReporterDraft" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "Thông tin phỏng vấn" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "Tiếp tục tạo bài" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "export function OfficerInfoForm" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "presiding" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "courtSecretary" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "participants" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "saveOfficerDraft" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "Thông tin vụ án" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "Tiếp tục tạo hồ sơ" features/workflows/officer/OfficerInfoForm.tsx
    - grep -q "bg-\[#1E3A8A\]" features/workflows/reporter/ReporterInfoForm.tsx
    - grep -q "bg-\[#1E3A8A\]" features/workflows/officer/OfficerInfoForm.tsx
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>Both form components render all required fields per D-05 and D-07, auto-save to localStorage, and use UI-SPEC styling.</done>
</task>

<task type="auto">
  <name>Task 2: Create MeetingLandingPage with workflow group cards</name>
  <files>components/MeetingLandingPage.tsx</files>
  <read_first>
    - App.tsx (lines 30-42 for WORKFLOW_CARDS, lines 1319-1340 for existing card rendering)
    - .planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-UI-SPEC.md (Workflow Group Card section, Language Selector section, Copywriting Contract)
    - .planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-CONTEXT.md (D-01, D-02, D-03)
    - features/workflows/types.ts
    - lib/auth.ts (for AuthUser type)
  </read_first>
  <action>
    **Per D-01, D-02, D-03:** Create `components/MeetingLandingPage.tsx` — the group-selection landing page that replaces the inline `/meeting` block.

    Props:
    ```typescript
    interface Props {
      user: AuthUser;
      navigate: (path: string) => void;
    }
    ```

    **Card mapping (D-03):**
    - reporter → "Bài phỏng vấn" / "Ghi chép & tổng hợp phỏng vấn báo chí"
    - specialist → "Thư ký họp" / "Biên bản cuộc họp chuyên nghiệp"
    - officer → "Thông tin vụ án" / "Ghi chép hồ sơ pháp lý"

    Import WORKFLOW_CARDS from inline definition (copy the constant, do NOT import from App.tsx to avoid circular dependency). Or import WorkflowGroup from features/workflows/types.ts and define cards locally.

    **Layout per UI-SPEC:**
    - Container: `max-w-4xl mx-auto p-8 space-y-8`
    - Page heading: "Chọn loại nội dung" — `text-2xl font-semibold text-slate-800` using `font-display` class (Space Grotesk)
    - Page subheading: "Chọn nhóm làm việc để bắt đầu" — `text-sm text-slate-600 mt-1`
    - Card grid: `grid grid-cols-1 md:grid-cols-3 gap-4`
    - Each card: `<button>` element (not div — per accessibility), classes: `flex flex-col gap-2 p-6 border border-slate-200 rounded-xl text-left bg-white hover:border-[#1E40AF] hover:shadow-sm transition-colors duration-200 cursor-pointer min-h-[44px]`
    - Card label: `text-base font-semibold text-slate-800`
    - Card description: `text-sm text-slate-500 mt-1`
    - onClick: `navigate('/' + card.group)`

    **Filter:** `WORKFLOW_CARDS.filter(c => user.workflowGroups?.includes(c.group))` — per D-01 only show groups user belongs to.

    **Empty state:** If no workflow groups: "Bạn chưa được phân vào nhóm nào. Liên hệ quản trị viên để được hỗ trợ." per UI-SPEC copywriting.

    **Language selector (from CONTEXT.md specifics):**
    Place a `<select>` below page heading, above cards. Label: "Ngôn ngữ phiên âm". Options: Tiếng Việt (vi), Tiếng Anh (en), Tiếng Trung (zh), Tiếng Hàn (ko), Tiếng Nhật (ja), -- (other). Default: vi.
    However per RESEARCH.md Open Question 2 recommendation: do NOT pass language as prop to workflow pages. Each workflow page manages its own audioLanguage state. So the language selector on MeetingLandingPage is just a visual placeholder — remove it. Instead, each workflow page will have its own language selector.

    REVISED: No language selector on MeetingLandingPage. Keep it simple: heading + cards only.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -c "error" || echo "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: components/MeetingLandingPage.tsx
    - grep -q "export function MeetingLandingPage" components/MeetingLandingPage.tsx
    - grep -q "Chọn loại nội dung" components/MeetingLandingPage.tsx
    - grep -q "Bài phỏng vấn" components/MeetingLandingPage.tsx
    - grep -q "Thư ký họp" components/MeetingLandingPage.tsx
    - grep -q "Thông tin vụ án" components/MeetingLandingPage.tsx
    - grep -q "workflowGroups" components/MeetingLandingPage.tsx
    - grep -q "<button" components/MeetingLandingPage.tsx (cards are button elements)
    - grep -q "hover:border-\[#1E40AF\]" components/MeetingLandingPage.tsx
    - grep -q "grid-cols-1 md:grid-cols-3" components/MeetingLandingPage.tsx
    - grep -q "navigate" components/MeetingLandingPage.tsx
    - grep -q "Bạn chưa được phân vào nhóm nào" components/MeetingLandingPage.tsx
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>MeetingLandingPage renders group cards filtered by user.workflowGroups, navigates to /{group} on click, shows empty state when no groups.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All 3 component files exist and export correctly
- Forms use UI-SPEC colors and copywriting
</verification>

<success_criteria>
- ReporterInfoForm with 5 fields per D-05
- OfficerInfoForm with 6 fields per D-07, including participant management
- MeetingLandingPage with filtered group cards per D-01/D-03
- All components follow UI-SPEC styling (colors, spacing, typography)
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-revamp-website-va-giao-dien-tung-nhom-tinh-nang/09-03-SUMMARY.md`
</output>
