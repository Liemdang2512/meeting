---
phase: 01-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/migrations/017_email_verification_and_google_oauth.sql
  - db/schema.sql
  - db/seed.sql
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "auth.users has email_verified_at and google_sub; verification tokens live in a dedicated auth table"
    - "Existing local seed admin and any row with password_hash is backfilled so login is not blocked"
    - "db/schema.sql matches migration for fresh installs"
  artifacts:
    - path: db/migrations/017_email_verification_and_google_oauth.sql
      provides: "Additive migration + backfill + indexes"
    - path: db/schema.sql
      provides: "Canonical CREATE/ALTER for auth.users and token table"
    - path: db/seed.sql
      provides: "Admin seed sets email_verified_at"
  key_links:
    - from: db/seed.sql
      to: auth.users
      via: "INSERT includes email_verified_at"
---

> **Alignment (2026-04-08 vs 01-RESEARCH.md):** No material task changes. Schema matches RESEARCH “email_verified_at + token table + google_sub”; OAuth code uses `google-auth-library` in 01-03 only.

<objective>
Add PostgreSQL schema support for email verification (timestamp + one-time token table) and Google account linking (`google_sub`), and keep local seed/admin users immediately verified.

Purpose: Backend and frontend work in later waves depend on these columns and tables.

Output: Migration `017`, updated `db/schema.sql`, updated `db/seed.sql`.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-auth/01-CONTEXT.md (locked: no JWT until verified; admin/seed verified; Resend for email)
@.planning/phases/01-auth/01-RESEARCH.md (email_verified_at, token table with token_hash, google_sub UNIQUE)
@db/schema.sql (current `auth.users`: id, email, password_hash, created_at)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 017 — columns, token table, backfill, indexes</name>
  <files>db/migrations/017_email_verification_and_google_oauth.sql</files>
  <read_first>
/Users/tanliem/Desktop/meeting-main/db/schema.sql
/Users/tanliem/Desktop/meeting-main/db/migrations/016_free_allowance_period.sql
  </read_first>
  <action>
Create `db/migrations/017_email_verification_and_google_oauth.sql` that:
1. `ALTER TABLE auth.users ADD COLUMN IF NOT EXISTS email_verified_at timestamptz NULL;`
2. `ALTER TABLE auth.users ADD COLUMN IF NOT EXISTS google_sub text NULL;` then `CREATE UNIQUE INDEX IF NOT EXISTS uq_auth_users_google_sub ON auth.users (google_sub) WHERE google_sub IS NOT NULL;` (partial unique so multiple NULLs allowed).
3. Ensure `password_hash` remains nullable (already `text` without NOT NULL in schema) — do not add NOT NULL.
4. `CREATE TABLE IF NOT EXISTS auth.email_verification_tokens (`  
   `id uuid PRIMARY KEY DEFAULT gen_random_uuid(),`  
   `user_id uuid NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,`  
   `token_hash text NOT NULL,`  
   `expires_at timestamptz NOT NULL,`  
   `used_at timestamptz NULL,`  
   `created_at timestamptz NOT NULL DEFAULT now()`  
   `);`  
   Index: `(user_id)`, `(token_hash)` unique on `token_hash`.
5. **Backfill (per CONTEXT / RESEARCH — avoid locking existing password users):**  
   `UPDATE auth.users SET email_verified_at = now() WHERE email_verified_at IS NULL AND password_hash IS NOT NULL;`
6. No new npm dependencies.

Rationale for backfill: Users created before this feature must keep logging in; only new self-service registrations stay unverified until they verify (Wave 2 sets NULL for new rows only).
  </action>
  <acceptance_criteria>
`grep -n "email_verified_at\|google_sub\|email_verification_tokens" /Users/tanliem/Desktop/meeting-main/db/migrations/017_email_verification_and_google_oauth.sql | wc -l` returns a number ≥ 8.
`grep -q "UPDATE auth.users SET email_verified_at" /Users/tanliem/Desktop/meeting-main/db/migrations/017_email_verification_and_google_oauth.sql` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -n "017_email_verification" /Users/tanliem/Desktop/meeting-main/db/migrations/017_email_verification_and_google_oauth.sql | head -1</automated>
  </verify>
  <done>Migration file exists and is idempotent (IF NOT EXISTS / IF NOT EXISTS columns where appropriate).</done>
</task>

<task type="auto">
  <name>Task 2: Sync db/schema.sql and db/seed.sql</name>
  <files>db/schema.sql, db/seed.sql</files>
  <read_first>
/Users/tanliem/Desktop/meeting-main/db/schema.sql
/Users/tanliem/Desktop/meeting-main/db/seed.sql
/Users/tanliem/Desktop/meeting-main/db/migrations/017_email_verification_and_google_oauth.sql
  </read_first>
  <action>
1. Update `db/schema.sql` `CREATE TABLE auth.users` block to include `email_verified_at timestamptz NULL` and `google_sub text NULL` (same order as migration). Append full `CREATE TABLE auth.email_verification_tokens` definition + indexes to match migration (for greenfield `schema.sql` runs).
2. Update `db/seed.sql` `INSERT INTO auth.users` to set `email_verified_at` to `NOW()` explicitly (add column to column list and values) so admin@local.dev is verified per CONTEXT D (admin/seed verified).
3. Do not change unrelated tables.
  </action>
  <acceptance_criteria>
`grep -q "email_verified_at" /Users/tanliem/Desktop/meeting-main/db/schema.sql` exits 0.
`grep -q "email_verification_tokens" /Users/tanliem/Desktop/meeting-main/db/schema.sql` exits 0.
`grep "INSERT INTO auth.users" /Users/tanliem/Desktop/meeting-main/db/seed.sql | grep -q "email_verified_at"` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>grep -c "email_verified_at" /Users/tanliem/Desktop/meeting-main/db/schema.sql /Users/tanliem/Desktop/meeting-main/db/seed.sql | awk -F: '{s+=$2} END {print s}' | grep -q '[1-9]'</automated>
  </verify>
  <done>Fresh schema + seed describe the same auth shape as migration 017.</done>
</task>

</tasks>

<verification>
Apply migration against local Docker Postgres (if available) or document `npm run db:migrate` equivalent used in repo; at minimum SQL files are consistent.
</verification>

<success_criteria>
Migration 017 + schema.sql + seed.sql align; backfill preserves existing password users.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth/01-01-SUMMARY.md`
</output>
