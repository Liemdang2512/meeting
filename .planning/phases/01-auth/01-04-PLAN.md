---
phase: 01-auth
plan: 04
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
files_modified:
  - lib/auth.ts
  - components/RegisterPage.tsx
  - components/LoginPage.tsx
  - App.tsx
  - .env.example
  - components/__tests__/LoginPage.test.tsx
  - components/__tests__/RegisterPage.test.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "After email registration, UI shows check-inbox message and does not store JWT or call onRegisterSuccess as logged-in"
    - "Login page shows working Google button linking to /api/auth/google (full URL in prod)"
    - "Login reads ?verified=1 to show success toast/message after email verify redirect; OAuth success lands on /meeting with cookies (no google=1 query required)"
  artifacts:
    - path: components/RegisterPage.tsx
      provides: "Success state 'Kiểm tra hộp thư'"
    - path: components/LoginPage.tsx
      provides: "Google CTA + verified query handling"
  key_links:
    - from: RegisterPage
      to: POST /api/auth/register
      via: "register() without setToken"
---

> **Alignment (2026-04-08 vs 01-RESEARCH.md):** Server OAuth uses **`google-auth-library`** (01-03); env **`GOOGLE_CLIENT_ID`**, **`GOOGLE_CLIENT_SECRET`**, **`API_PUBLIC_URL`** for redirect URI. Default-safe linking blocks auto-merge with password accounts — optional: if callback redirects to `/login?oauth_error=...`, show a short Vietnamese hint (not required if server returns JSON-only for callback).

<objective>
Update the SPA for verified registration UX, query-param feedback after email verification and Google OAuth, and enable the Google sign-in button using the existing API base URL pattern.

Purpose: CONTEXT specifics — register success = inbox instructions; login = Google button + callback handling via redirect to APP_URL.

Output: `lib/auth.ts` contract, RegisterPage/LoginPage/App.tsx updates, component tests, `.env.example` notes for Google redirect URIs (frontend + backend).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/01-auth/01-RESEARCH.md (frontend: start URL hits `/api/auth/google`; redirect URI is server-side `API_PUBLIC_URL` + `/api/auth/google/callback`)
@lib/api.ts (`API_BASE` = `/api` in dev or `VITE_API_URL` + `/api`)
@lib/auth.ts (`register`, `login`)
@components/LoginPage.tsx (Google button currently disabled)
@components/RegisterPage.tsx
@App.tsx (lines ~803–877 unauthenticated routes)
</context>

<tasks>

<task type="auto">
  <name>Task 1: lib/auth.register + RegisterPage success flow + App wiring</name>
  <files>lib/auth.ts, components/RegisterPage.tsx, App.tsx, .env.example</files>
  <read_first>
/Users/tanliem/Desktop/meeting-main/lib/auth.ts
/Users/tanliem/Desktop/meeting-main/lib/api.ts
/Users/tanliem/Desktop/meeting-main/components/RegisterPage.tsx
/Users/tanliem/Desktop/meeting-main/App.tsx
  </read_first>
  <action>
**`lib/auth.ts`**  
- Change `register()` return type to `Promise<{ ok: true; message: string }>` (or `void` + throw on error — if void, document that caller must not expect token). **Must not** call `setToken` on success.  
- Parse JSON: expect `ok` and `message` on 201.

**`RegisterPage.tsx`**  
- On successful `register()`, set local state `registrationComplete: true` (or equivalent).  
- Render Vietnamese copy per CONTEXT: hướng dẫn **"Kiểm tra hộp thư"** / đã gửi link xác nhận; **do not** invoke `onRegisterSuccess` (user is not logged in).  
- Provide button "Đến trang đăng nhập" → `onGoToLogin()`.

**`App.tsx`**  
- Remove `onRegisterSuccess` async block that calls `getMe()` and navigates to `/meeting` after register (no longer valid). Pass a no-op or remove prop if component interface simplified — **RegisterPage** should not require login success callback; adjust props: e.g. drop `onRegisterSuccess` or keep for future use but unused (prefer remove/cleanup to avoid confusion).

**`.env.example`**  
- Add comment block: Google Cloud **Authorized redirect URIs** must include `{API_PUBLIC_URL}/api/auth/google/callback`; **JavaScript origins** may include `{APP_URL}` (e.g. http://localhost:3000).
  </action>
  <acceptance_criteria>
`node -e "const fs=require('fs');const s=fs.readFileSync('/Users/tanliem/Desktop/meeting-main/lib/auth.ts','utf8');const i=s.indexOf('export async function register');const j=s.indexOf('export async function',i+1);const body=s.slice(i,j);if(/setToken/.test(body))process.exit(1);"` exits 0 (register() body contains no `setToken`).
`grep -n "Kiểm tra hộp thư\|hộp thư" /Users/tanliem/Desktop/meeting-main/components/RegisterPage.tsx` exits 0.
(Task 2 owns `RegisterPage.test.tsx`: assert `onRegisterSuccess` not called after register success — avoid Task 1 verify requiring tests before mocks are updated.)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tanliem/Desktop/meeting-main && node -e "const fs=require('fs');const s=fs.readFileSync('lib/auth.ts','utf8');const i=s.indexOf('export async function register');const j=s.indexOf('export async function',i+1);const body=s.slice(i,j===-1?s.length:j);if(/setToken/.test(body))process.exit(1);" && grep -qE "Kiểm tra hộp thư|hộp thư" components/RegisterPage.tsx</automated>
  </verify>
  <done>Register flow matches API from 01-02.</done>
</task>

<task type="auto">
  <name>Task 2: LoginPage — Google + verified query params + tests</name>
  <files>components/LoginPage.tsx, components/__tests__/LoginPage.test.tsx, components/__tests__/RegisterPage.test.tsx, App.tsx, lib/auth.ts</files>
  <read_first>
/Users/tanliem/Desktop/meeting-main/components/LoginPage.tsx
/Users/tanliem/Desktop/meeting-main/components/__tests__/LoginPage.test.tsx
/Users/tanliem/Desktop/meeting-main/lib/api.ts
  </read_first>
  <action>
**Google button**  
- Enable the first social button (Google). `onClick`: `window.location.href = `${import.meta.env.DEV ? '' : (import.meta.env.VITE_API_URL ?? '')}/api/auth/google`` — **fix**: match `authFetch` base: in dev use **`/api/auth/google`** (relative to origin); in prod use **`${import.meta.env.VITE_API_URL}/api/auth/google`** when `VITE_API_URL` set, else `/api/auth/google`. Extract small helper in `lib/auth.ts` e.g. `export function getGoogleOAuthStartUrl(): string` to avoid duplication.

**Query params (after redirects from server)**  
- On mount, `useEffect` reads `URLSearchParams` from `window.location.search`:  
  - `verified=1` → show success banner: e.g. "Email đã được xác nhận. Bạn có thể đăng nhập."  
  - After Google OAuth, server redirects to **`/meeting`** (01-03); no `google=1` on login is required. Optional: handle legacy `google=1` with a short message + `replaceState`.

**`LoginPage.test.tsx`**  
- Mock `getGoogleOAuthStartUrl` or `window.location` assign spy: button click sets `location.href` containing `/api/auth/google`.  
- Test: unverified login error from API shows server message (mock `login` rejection with `Vui lòng xác nhận email trước khi đăng nhập.`).

**`RegisterPage.test.tsx`**  
- Update mocks: `register` resolves without token; assert success UI contains inbox / verify messaging; assert `onRegisterSuccess` **not** called (use `vi.fn()` / `not.toHaveBeenCalled()` or `toHaveBeenCalledTimes(0)` — satisfy grep on this file for CI/docs if used).

**`App.tsx`**  
- OAuth callback ends on `/meeting` with cookies set; ensure `getMe()` in bootstrap runs so user state loads. Keep existing `if (user && route === '/login')` redirect to `/meeting` for password login after verify.
  </action>
  <acceptance_criteria>
`grep -n "Sắp ra mắt" /Users/tanliem/Desktop/meeting-main/components/LoginPage.tsx` — Google button must **not** be disabled with "Sắp ra mắt" for the Google button (Microsoft may stay disabled).
`grep -n "api/auth/google" /Users/tanliem/Desktop/meeting-main/components/LoginPage.tsx /Users/tanliem/Desktop/meeting-main/lib/auth.ts` — at least one match.
`grep -qE "not\\.toHaveBeenCalled|toHaveBeenCalledTimes\\(0\\)" /Users/tanliem/Desktop/meeting-main/components/__tests__/RegisterPage.test.tsx` exits 0 (register success does not call `onRegisterSuccess`).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/tanliem/Desktop/meeting-main && npm test -- --run components/__tests__/LoginPage.test.tsx components/__tests__/RegisterPage.test.tsx</automated>
  </verify>
  <done>Component tests green; Google URL works in dev and production build config.</done>
</task>

</tasks>

<verification>
`npm test` passes project-wide.
</verification>

<success_criteria>
Frontend aligns with email verification + Google OAuth server behavior; tests updated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth/01-04-SUMMARY.md`
</output>
