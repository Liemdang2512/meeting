---
phase: 01-auth
plan: 05
type: execute
wave: 5
depends_on:
  - 01-02
  - 01-04
files_modified:
  - server/lib/getResendConfig.ts
  - server/lib/sendVerificationEmail.ts
  - server/routes/email.ts
  - server/routes/admin.ts
  - server/routes/__tests__/email.test.ts
  - features/workflows/shared/SharedMeetingWorkflow.tsx
  - .env.example
  - .planning/ROADMAP.md
autonomous: true
requirements: []
user_setup:
  - Chạy migration Postgres (gồm bảng app_settings và cột auth liên quan verify/OAuth).
  - Cấu hình Resend: env hoặc Admin → Cài đặt email.
must_haves:
  truths:
    - "getResendConfig ưu tiên env RESEND_API_KEY rồi mới đọc app_settings.resend_api_key"
    - "Admin UI email settings cho Resend + email_max_recipients; không còn Gmail SMTP giả trong UI"
  artifacts:
    - path: server/lib/getResendConfig.ts
      provides: "getResendConfig + getMaxEmailRecipients"
    - path: features/workflows/shared/SharedMeetingWorkflow.tsx
      provides: "EmailSettingsSection Resend"
---

<objective>
Gộp Phase 1 với cấu hình vận hành email + DB: một nguồn Resend thống nhất (env hoặc `app_settings`), giới hạn người nhận từ env hoặc DB, UI admin khớp backend thực tế.

Purpose: Tránh lệch giữa UI Gmail và Resend; cho phép chỉ cấu hình qua Admin khi chưa đặt env.

Output: `getResendConfig.ts`, cập nhật `email.ts`, `sendVerificationEmail.ts`, `admin` ALLOWED_KEYS + mask, `EmailSettingsSection`, test + ROADMAP.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: getResendConfig + getMaxEmailRecipients</name>
  <files>server/lib/getResendConfig.ts</files>
  <action>
Implement async helpers: env-first for API key and from; DB fallback keys `resend_api_key`, `resend_from`; max recipients env `SMTP_MAX_RECIPIENTS` then `email_max_recipients` setting; safe catch when sql fails (tests).
  </action>
</task>

<task type="auto">
  <name>Task 2: Wire send-minutes + verification email</name>
  <files>server/routes/email.ts, server/lib/sendVerificationEmail.ts</files>
  <action>
Replace direct `process.env` usage with helpers; 503 message hướng dẫn cả env và admin.
  </action>
</task>

<task type="auto">
  <name>Task 3: Admin API + UI</name>
  <files>server/routes/admin.ts, features/workflows/shared/SharedMeetingWorkflow.tsx</files>
  <action>
ALLOWED_KEYS thêm `resend_api_key`, `resend_from`; GET mask secret giống gmail_app_password; EmailSettingsSection: form Resend + max recipients, copy tiếng Việt về ưu tiên env.
  </action>
</task>

<task type="auto">
  <name>Task 4: Tests + docs</name>
  <files>server/routes/__tests__/email.test.ts, .env.example, .planning/ROADMAP.md</files>
  <action>
mockSql default []; cập nhật message 503; thêm case đọc key từ app_settings; PUT resend key; ghi chú .env.example; ROADMAP Phase 1 success criteria.
  </action>
</task>

</tasks>
