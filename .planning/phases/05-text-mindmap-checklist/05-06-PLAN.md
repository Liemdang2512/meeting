---
phase: 05-text-mindmap-checklist
plan: "06"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - features/mindmap/lib/mindmapSchema.ts
  - features/mindmap/hooks/useMindmapFromText.ts
  - features/mindmap/components/MindmapCanvas.tsx
autonomous: true
requirements: [MINDMAP-ICON-01]
must_haves:
  truths:
    - "Mindmap nodes (depth > 0) hiển thị icon SVG làm nền mờ bên trong card"
    - "Gemini trả về iconKey cho mỗi node từ whitelist 30 key"
    - "Icon fallback về Circle khi key không map được"
    - "Root node không có icon"
    - "npm run build và npm test pass sau khi thêm icon feature"
  artifacts:
    - path: "features/mindmap/lib/mindmapSchema.ts"
      provides: "iconKey?: string field trong Branch2Schema, BranchSchema, MindmapNode interface, toMindmapTree"
    - path: "features/mindmap/hooks/useMindmapFromText.ts"
      provides: "buildMindmapPrompt updated với whitelist 30 keys và instruction chọn iconKey"
    - path: "features/mindmap/components/MindmapCanvas.tsx"
      provides: "MindmapNodeData.iconKey field + icon background rendering trong MindmapNodeComponent"
  key_links:
    - from: "features/mindmap/lib/mindmapSchema.ts"
      to: "features/mindmap/components/MindmapCanvas.tsx"
      via: "MindmapNode.iconKey passed through toMindmapTree → node data"
      pattern: "iconKey"
    - from: "features/mindmap/hooks/useMindmapFromText.ts"
      to: "Gemini API"
      via: "prompt chứa whitelist và instruction iconKey"
      pattern: "iconKey"
---

<objective>
Thêm icon ngữ cảnh (lucide-react SVG) vào mindmap nodes: Gemini chọn `iconKey` từ whitelist 30 key, icon hiển thị dạng nền mờ (15% opacity, 40px) phía sau label text trong mỗi node card (depth > 0). Root node không có icon.

Purpose: Cải thiện visual UX — người dùng nhận ra loại thông tin của node ngay lập tức qua icon, không chỉ qua màu sắc.
Output: Ba file được cập nhật (schema + prompt + canvas) + lucide-react installed.
</objective>

<execution_context>
@/Users/tanliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tanliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-text-mindmap-checklist/05-CONTEXT.md
@.planning/phases/05-text-mindmap-checklist/05-05-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From features/mindmap/lib/mindmapSchema.ts (hiện tại — cần cập nhật):
```typescript
const Branch2Schema = z.object({
  id: z.string().optional(),
  label: z.string(),
  // iconKey?: string  ← CẦN THÊM
});

const BranchSchema = z.object({
  id: z.string().optional(),
  label: z.string(),
  children: z.array(Branch2Schema).optional(),
  // iconKey?: string  ← CẦN THÊM
});

export interface MindmapNode {
  id: string;
  label: string;
  children: MindmapNode[];
  // iconKey?: string  ← CẦN THÊM
}

export function toMindmapTree(response: MindmapResponse): MindmapNode
// Cần pass iconKey từ branch/branch2 vào returned MindmapNode objects
```

From features/mindmap/components/MindmapCanvas.tsx (hiện tại — cần cập nhật):
```typescript
interface MindmapNodeData {
  label: string;
  depth: number;
  side: 'left' | 'right' | 'center';
  colorIdx: number;
  hasChildren: boolean;
  expanded: boolean;
  onToggle: (id: string) => void;
  // iconKey?: string  ← CẦN THÊM
}

// MindmapNodeComponent: hiện render label + toggle button
// CẦN THÊM: icon background khi !isCenter && iconKey present
```

From features/mindmap/hooks/useMindmapFromText.ts (hiện tại — cần cập nhật):
```typescript
function buildMindmapPrompt(text: string): string
// Hiện tại prompt không đề cập iconKey
// CẦN: thêm whitelist + instruction chọn iconKey cho mỗi node (branch và sub-branch)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install lucide-react + update mindmapSchema với iconKey</name>
  <files>package.json, features/mindmap/lib/mindmapSchema.ts</files>
  <action>
**Step 1 — Install lucide-react:**
```bash
npm install lucide-react
```
Verify `lucide-react` xuất hiện trong `dependencies` của `package.json`.

**Step 2 — Update mindmapSchema.ts:**

Thêm `iconKey?: z.string().optional()` vào `Branch2Schema` và `BranchSchema`:

```typescript
const Branch2Schema = z.object({
  id: z.string().optional(),
  label: z.string(),
  iconKey: z.string().optional(),
});

const BranchSchema = z.object({
  id: z.string().optional(),
  label: z.string(),
  children: z.array(Branch2Schema).optional(),
  iconKey: z.string().optional(),
});
```

Cập nhật `MindmapNode` interface để thêm `iconKey`:

```typescript
export interface MindmapNode {
  id: string;
  label: string;
  children: MindmapNode[];
  iconKey?: string;
}
```

Cập nhật `toMindmapTree` để pass `iconKey` từ branch/sub-branch vào returned nodes. Trong phần map branch:
```typescript
return {
  id: branchId,
  label: branch.label,
  iconKey: branch.iconKey,   // ← thêm dòng này
  children: (branch.children ?? []).map((sub, si) => {
    const subId = nextId(`b${bi}s${si}`, sub.id);
    return {
      id: subId,
      label: sub.label,
      iconKey: sub.iconKey,  // ← thêm dòng này
      children: [],
    };
  }),
};
```

Root node không nhận iconKey (đúng với design decision: root không có icon).
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/meeting-main && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `lucide-react` có trong `package.json` dependencies
    - `Branch2Schema` và `BranchSchema` có `iconKey: z.string().optional()`
    - `MindmapNode` interface có `iconKey?: string`
    - `toMindmapTree` pass `iconKey` cho branch và sub-branch nodes
    - `npx tsc --noEmit` pass không có lỗi liên quan đến iconKey
  </done>
</task>

<task type="auto">
  <name>Task 2: Update useMindmapFromText prompt với iconKey whitelist</name>
  <files>features/mindmap/hooks/useMindmapFromText.ts</files>
  <action>
Cập nhật hàm `buildMindmapPrompt` trong `features/mindmap/hooks/useMindmapFromText.ts` để yêu cầu Gemini trả về `iconKey` cho mỗi branch và sub-branch node.

Thêm section về `iconKey` vào prompt, ngay trước phần "CHỈ trả về JSON":

```
ICON RULE:
- Mỗi nhánh chính và nhánh con (KHÔNG áp dụng cho root) phải có trường "iconKey"
- Chọn iconKey phù hợp nhất với nội dung node từ danh sách sau (chỉ dùng các key trong danh sách):
  briefcase, alert-triangle, dollar-sign, check-circle, users, target, clock,
  file-text, settings, zap, shield, trending-up, map, list, message-circle,
  calendar, database, lock, star, flag, package, tool, globe, heart, eye,
  bar-chart, layers, link, search, upload
- Nếu không có key phù hợp, dùng "list" làm mặc định
```

Cập nhật ví dụ JSON ở cuối prompt để minh họa `iconKey`:
```json
{"root":{"label":"Chủ đề trung tâm","children":[{"label":"Nhánh chính 1","iconKey":"briefcase","children":[{"label":"Chi tiết 1.1","iconKey":"target"},{"label":"Chi tiết 1.2","iconKey":"clock"}]},{"label":"Nhánh chính 2","iconKey":"users","children":[{"label":"Chi tiết 2.1","iconKey":"check-circle"}]}]}}
```

Không thay đổi các phần khác của prompt (NHIỆM VỤ, NGUYÊN TẮC PHÂN NHÁNH).
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/meeting-main && npx tsc --noEmit 2>&1 | head -20 && npm run test:unit -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `buildMindmapPrompt` chứa danh sách 30 whitelist keys
    - Prompt có instruction rõ ràng về `iconKey` cho branch và sub-branch (không áp dụng root)
    - Ví dụ JSON trong prompt minh họa `iconKey` đúng format
    - Unit tests hiện có vẫn pass (mock responses không bị ảnh hưởng vì iconKey là optional)
  </done>
</task>

<task type="auto">
  <name>Task 3: Update MindmapCanvas để render icon background</name>
  <files>features/mindmap/components/MindmapCanvas.tsx</files>
  <action>
**Step 1 — Import lucide-react icons:**

Thêm import ở đầu file sau các import hiện có:

```typescript
import type { LucideIcon } from 'lucide-react';
import {
  Briefcase, AlertTriangle, DollarSign, CheckCircle, Users, Target, Clock,
  FileText, Settings, Zap, Shield, TrendingUp, Map, List, MessageCircle,
  Calendar, Database, Lock, Star, Flag, Package, Tool, Globe, Heart, Eye,
  BarChart, Layers, Link, Search, Upload, Circle,
} from 'lucide-react';
```

**Step 2 — Tạo ICON_MAP object** (sau phần BRANCH_PALETTE):

```typescript
const ICON_MAP: Record<string, LucideIcon> = {
  'briefcase': Briefcase,
  'alert-triangle': AlertTriangle,
  'dollar-sign': DollarSign,
  'check-circle': CheckCircle,
  'users': Users,
  'target': Target,
  'clock': Clock,
  'file-text': FileText,
  'settings': Settings,
  'zap': Zap,
  'shield': Shield,
  'trending-up': TrendingUp,
  'map': Map,
  'list': List,
  'message-circle': MessageCircle,
  'calendar': Calendar,
  'database': Database,
  'lock': Lock,
  'star': Star,
  'flag': Flag,
  'package': Package,
  'tool': Tool,
  'globe': Globe,
  'heart': Heart,
  'eye': Eye,
  'bar-chart': BarChart,
  'layers': Layers,
  'link': Link,
  'search': Search,
  'upload': Upload,
};

const DEFAULT_ICON: LucideIcon = Circle;
```

**Step 3 — Thêm `iconKey` vào `MindmapNodeData` interface:**

```typescript
interface MindmapNodeData {
  label: string;
  depth: number;
  side: 'left' | 'right' | 'center';
  colorIdx: number;
  hasChildren: boolean;
  expanded: boolean;
  onToggle: (id: string) => void;
  iconKey?: string;  // ← thêm field này
}
```

**Step 4 — Render icon background trong `MindmapNodeComponent`:**

Trong hàm `MindmapNodeComponent`, ngay trước `return`, thêm logic lấy icon:

```typescript
const IconComponent = !isCenter && d.iconKey
  ? (ICON_MAP[d.iconKey] ?? DEFAULT_ICON)
  : null;
```

Trong JSX của `MindmapNodeComponent`, thay đổi wrapper `<div>` để dùng `position: relative` và thêm icon background element:

```tsx
return (
  <div
    style={{ ...style, display: 'flex', alignItems: 'center', gap: 4, wordBreak: 'break-word', position: 'relative', overflow: 'hidden' }}
    onClick={() => d.hasChildren && d.onToggle(id)}
  >
    {/* Icon background — chỉ cho non-root nodes */}
    {IconComponent && (
      <div style={{
        position: 'absolute',
        right: 6,
        top: '50%',
        transform: 'translateY(-50%)',
        opacity: 0.15,
        pointerEvents: 'none',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}>
        <IconComponent size={40} />
      </div>
    )}

    {/* Existing handle elements — giữ nguyên */}
    {isCenter && ( ... )}
    {isLeft && !isCenter && ( ... )}
    {!isLeft && !isCenter && ( ... )}

    <span style={{ flex: 1 }}>{d.label}</span>

    {d.hasChildren && !isCenter && (
      <span style={{ fontSize: 8, opacity: 0.45, flexShrink: 0, marginLeft: 2 }}>
        {d.expanded ? (isLeft ? '►' : '◄') : (isLeft ? '◄' : '►')}
      </span>
    )}
  </div>
);
```

**Step 5 — Pass `iconKey` từ tree node vào React Flow node data:**

Trong hàm `layoutSubtree` bên trong `buildBidirectionalLayout`, cập nhật `data` object để include `iconKey`:

```typescript
data: {
  label:       node.label,
  depth,
  side,
  colorIdx,
  hasChildren: node.children.length > 0,
  expanded:    !collapsed.has(node.id),
  onToggle:    () => {},
  iconKey:     node.iconKey,   // ← thêm field này
} as unknown as Record<string, unknown>,
```

Root node (trong `rfNodes.push` đầu tiên) không cần `iconKey` vì `isCenter` sẽ skip render icon.

**Lưu ý:** Giữ nguyên toàn bộ logic export PNG/PDF, layout algorithm, handle rendering, edge styling. Chỉ thêm icon background.
  </action>
  <verify>
    <automated>cd /Users/tanliem/Desktop/meeting-main && npx tsc --noEmit 2>&1 | head -30 && npm run test:unit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `ICON_MAP` có đầy đủ 30 keys từ whitelist
    - `MindmapNodeData` có `iconKey?: string`
    - Non-root nodes hiển thị icon background với opacity 0.15, size 40px, positioned absolute ở right-center của card
    - Root node (isCenter) không render icon
    - Fallback về `Circle` khi iconKey không match
    - `iconKey` được pass từ `layoutSubtree` vào node data
    - `npx tsc --noEmit` pass
    - `npm run test:unit` pass (không có test nào break)
    - `npm run build` thành công
  </done>
</task>

</tasks>

<verification>
Sau khi hoàn thành 3 tasks:

1. `npm run build` thành công, không có TypeScript error
2. `npm run test:unit` pass — đặc biệt 16 mindmap tests không bị break bởi `iconKey` optional field
3. `lucide-react` có trong `package.json` dependencies
4. File `mindmapSchema.ts` có `iconKey?: z.string().optional()` trong Branch/Branch2 schemas
5. File `useMindmapFromText.ts` prompt chứa 30 whitelist keys và instruction về iconKey
6. File `MindmapCanvas.tsx` render `<IconComponent size={40} />` với opacity 0.15 cho non-root nodes

**Manual smoke test (sau khi chạy npm run dev):**
- Vào `/mindmap`, paste một đoạn văn bản, click "Tạo mindmap"
- Quan sát: các branch nodes (depth 1, 2) có icon mờ phía sau text
- Root node (center) không có icon
- Expand/collapse, zoom/pan vẫn hoạt động bình thường
- Export PNG/PDF vẫn hoạt động (icon được capture trong screenshot)
</verification>

<success_criteria>
- `lucide-react` installed và imported đúng cách
- Tất cả branch nodes (depth >= 1) trong mindmap canvas hiển thị icon SVG làm background với opacity 0.15
- Root node (center) không có icon
- Gemini được prompt để chọn iconKey từ whitelist 30 keys cho mỗi non-root node
- Fallback icon (Circle) được dùng khi key không map được
- Không có TypeScript errors (`npx tsc --noEmit` clean)
- Tất cả 16+ unit tests hiện có vẫn pass (iconKey là optional, không break existing mocks)
- `npm run build` thành công
</success_criteria>

<output>
After completion, create `.planning/phases/05-text-mindmap-checklist/05-06-SUMMARY.md`
</output>
