# Quick 260408-mu1: New users get 1000 free credits per month

## Tasks

1. **Free monthly allowance module** — `server/billing/freeMonthlyAllowance.ts`: 1000 credits per UTC calendar month for `role = free` and empty `workflow_groups`; idempotent `wallet_ledger` row `free-allowance:{userId}:{YYYY-MM}`.

2. **Wire entry points** — Registration (`auth.ts` tx), login, refresh, `GET /api/quota`, and `authorizeAndCharge` (month rollover before debit).

3. **Tests** — `server/billing/__tests__/freeMonthlyAllowance.integration.test.ts` for grant, idempotency, and paid-plan exclusion.
