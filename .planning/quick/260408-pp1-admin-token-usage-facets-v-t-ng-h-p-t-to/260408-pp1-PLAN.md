# Quick 260408-pp1 — Admin token-usage: facets & aggregate từ DB

## Tasks

1. **Hook + admin UI** — `facets !== null` thay vì kiểm tra `.length` để không fallback sang `logs` khi server trả facet rỗng; thêm `aggregateScope` để phân biệt tổng hợp server vs trang.
2. **Overview copy** — Mô tả đúng phạm vi dữ liệu theo `aggregateScope`.
3. **API** — `GET /token-logs/summary` `byUser` giới hạn 5 (đủ cho Top 5 UI).

## Verify

- `npm test`
- Admin `/admin/token-usage`: sau khi summary OK, dropdown lọc dùng facet server; thẻ tổng token khớp tổng DB (theo lọc).
